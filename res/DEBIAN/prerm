#!/bin/bash

set -e

case $1 in
    remove|upgrade)
		INITSYS=$(ls -al /proc/1/exe | awk -F' ' '{print $NF}' | awk -F'/' '{print $NF}')
		rm -f /usr/bin/ganwei-remotedesk
		
		if [ "systemd" == "${INITSYS}" ]; then

			systemctl stop ganwei-remotedesk || true
			systemctl disable ganwei-remotedesk || true
			rm /etc/systemd/system/ganwei-remotedesk.service /usr/lib/systemd/system/ganwei-remotedesk.service || true
			
			# workaround temp dev build between 1.1.9 and 1.2.0
			serverUser=$(ps -ef | grep -E 'ganwei-remotedesk +--server' | grep -v 'sudo ' | awk '{print $1}' | head -1)
			if [ "$serverUser" != "" ] && [ "$serverUser" != "root" ]
			then
				systemctl --machine=${serverUser}@.host --user stop ganwei-remotedesk >/dev/null 2>&1 || true
			fi
			rm /usr/lib/systemd/user/ganwei-remotedesk.service >/dev/null 2>&1 || true
		fi
        ;;
esac

exit 0
